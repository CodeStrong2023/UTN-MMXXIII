let x, y; //definir varias variables en una misma linea como en JAVA
x = 10, y=21 ; //asignar valor a x a la vez que se define y varias a la vez

let z = x+y;//se asigna el valor de la operacion

console.log(z);

//Javascript es case sensitive como Java

//Los nombres de las variables deben ser descriptivos y no pueden empezar con numeros
//se puede escribir antes el signo peso 
let $1numero = 10;
console.log($1numero);

//se puede escribir antes el signo guion bajo
//no se pueden usar nombres de variables con palabras reservadas
